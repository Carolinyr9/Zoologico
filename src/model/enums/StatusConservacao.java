package model.enums;

public enum StatusConservacao {
    EXTINTO,                // Extinct (EX)
    EXTINTO_NA_NATUREZA,    // Extinct in the Wild (EW)
    CRITICAMENTE_EM_PERIGO, // Critically Endangered (CR)
    EM_PERIGO,              // Endangered (EN)
    VULNERAVEL,             // Vulnerable (VU)
    QUASE_AMEAÇADO,         // Near Threatened (NT)
    MENOR_RISCO,            // Least Concern (LC)
    DADOS_INSUFICIENTES,    // Data Deficient (DD)
    NAO_AVALIADO            // Not Evaluated (NE)
}
